bitmap
======

To run it: 
```
$ cd /path/to/cloned_repo
$ bundle install
$ bundle exec ./bin/bitmap.rb
```

To run the test suite: 
```
$ cd /path/to/cloned_repo
$ bundle install
$ bundle exec rspec
```
